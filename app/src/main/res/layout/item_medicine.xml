<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:background="?selectableItemBackground"
	android:minHeight="72dp"
	android:orientation="horizontal"
	android:padding="16dp"
	tools:context=".screen.list.MedicinesListAdapter">

	<View
		android:id="@+id/view_medicine_placeholder"
		android:layout_width="40dp"
		android:layout_height="40dp"
		android:background="#4D8E8E8E"/>

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_gravity="center_vertical"
		android:layout_marginStart="16dp"
		android:orientation="vertical">

		<TextView
			android:id="@+id/tv_medicine_trade_label"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:ellipsize="end"
			android:maxLines="1"
			android:textColor="?colorOnGeneral"
			android:textSize="16sp"
			tools:text="@tools:sample/lorem/random"/>

		<TextView
			android:id="@+id/tv_medicine_manufacturer_name"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:layout_marginTop="4dp"
			android:ellipsize="end"
			android:maxLines="1"
			android:textColor="?colorOnGeneralVariant"
			android:textSize="12sp"
			tools:text="@tools:sample/lorem/random"/>
	</LinearLayout>
</LinearLayout>